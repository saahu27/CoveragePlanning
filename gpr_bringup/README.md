Launch and config for GPR coverage. **Docs:** [`../docs/README.md`](../docs/README.md).

```bash
ros2 launch gpr_bringup gpr_coverage.launch.py
```

Key paths: `config/gpr_coverage.yaml` · `launch/gpr_coverage.launch.py` · `rviz/gpr_scan.rviz` · `worlds/`
